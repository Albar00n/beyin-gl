/**
 * Class that can be used to track long presses on an HTML Element. A long press is a press that
 * lasts a minimum duration (see the [[timeout]] member) while the pointer is not moved more than a
 * certain threshold (see the [[moveThreshold]] member).
 */
export declare class LongPressHandler {
    readonly element: HTMLElement;
    onLongPress: (event: MouseEvent | Touch) => void;
    onTap?: ((event: MouseEvent | Touch) => void) | undefined;
    /**
     * How long to wait (in ms) until a press is considered a long press.
     */
    timeout: number;
    /**
     * If the cursor moves more than the given number of pixels, it is not a long-press, but a pan.
     */
    moveThreshold: number;
    /**
     * Mouse button id that should be handled by this event.
     */
    buttonId: number;
    private m_pressEvent?;
    private m_timerId?;
    private m_moveHandlerRegistered;
    private readonly m_boundPointerMoveHandler;
    private readonly m_boundMouseDownHandler;
    private readonly m_boundTouchStartHandler;
    private readonly m_boundMouseUpHandler;
    private readonly m_boundTouchEndHandler;
    /**
     * Default constructor.
     *
     * @param element - The HTML element to track.
     * @param onLongPress - The callback to call when a long press occurred.
     * @param onTap - Optional callback to call on a tap, i.e. when the press ends before the
     * specified timeout.
     */
    constructor(element: HTMLElement, onLongPress: (event: MouseEvent | Touch) => void, onTap?: ((event: MouseEvent | Touch) => void) | undefined);
    /**
     * Removes all events listeners. No more events will be sent.
     */
    dispose(): void;
    private startPress;
    private onMouseDown;
    private onTouchStart;
    private onMouseUp;
    private onTouchEnd;
    private onPointerMove;
    private cancel;
    private cancelTimer;
    private addPointerMoveHandler;
    private removePointerMoveHandler;
    private onTimeout;
}
//# sourceMappingURL=LongPressHandler.d.ts.map